import React from "react";
import { PlainPanel } from "components/panels/plainPanel";
import { useBullets } from "../hooks/useBullet";

export const BulletPanel = () => {
  return <PlainPanel title="Bullet">Bullet</PlainPanel>;
};
