import { PlainPanel } from "components/panels/plainPanel";
import React from "react";

export const WeatherPanel = () => {
  return (
    <PlainPanel title="WeatherPanel">
      <div>WeatherPanel</div>
    </PlainPanel>
  );
};
